# Milestone 0 技术探针报告

| 项目 | 内容 |
|---|---|
| 状态 | In Progress |
| 日期 | 2026-06-09 |
| 当前结论 | MoonBit 核心路径初步可行，Gate 1 尚未通过 |

## 已验证证据

| 探针 | 当前证据 | 结果 |
|---|---|---|
| MoonBit 工程 | 当前清单格式、严格检查、格式检查和测试 | Pass |
| 地址 | A1 列地址 `A`、`AA`、`XFD` 双向转换与越界拒绝 | Pass |
| 公式扫描 | 固定高风险公式可识别结构、运算符、标点和 A1 引用 | Partial |
| 循环 | 自循环、三节点循环和无环链可稳定区分 | Pass |
| 增量传播 | 只统计依赖编辑输入的单元格；值和诊断不变时停止传播 | Partial |
| 多错误 | 根错误优先级与同级稳定选择 | Partial |
| 浏览器边界 | MoonBit JS 目标返回稳定序列化状态；无效草稿不触发重算 | Partial |

## 当前自动化结果

```text
moon check --deny-warn
moon test
moon test --target js
moon fmt --check
```

当前共有 16 个测试，默认目标和 JS 目标全部通过。

## Gate 1 剩余条件

- 实现固定公式的真实 Token、Typed AST 和 UTF-8 源码范围，而不只是结构探针。
- 在 1,000 单元格菱形依赖图上验证准确重算集合，并与全量结果比较。
- 输出稳定循环路径和包含距离、地址顺序的完整多根因排序。
- 建立真实最小浏览器页面，调用 MoonBit JS 核心并展示结果与诊断。
- 记录 Wasm 同输入正确性、性能和开发复杂度。

完成以上前，项目保持 Milestone 0，不进入全面 MVP 开发。
